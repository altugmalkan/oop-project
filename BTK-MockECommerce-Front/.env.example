# Market Hub Dashboard - Environment Variables Example
# Copy this file to .env.local and fill in the actual values

# =============================================================================
# CLIENT ENVIRONMENT VARIABLES (VITE_* prefix required)
# These variables will be exposed to the browser
# =============================================================================

# API Configuration
VITE_API_BASE_URL=http://localhost:3000/api/v1

# App Configuration  
VITE_APP_NAME=Market Hub Dashboard
VITE_APP_VERSION=1.0.0

# Development Tools
VITE_ENABLE_DEV_TOOLS=true

# =============================================================================
# SERVER ENVIRONMENT VARIABLES
# These variables are only available at build time and server-side
# =============================================================================

# Node Environment
NODE_ENV=development

# =============================================================================
# NOTES:
# =============================================================================
# 
# 1. Only VITE_* prefixed variables are accessible in client-side code
# 2. Never put secrets in VITE_* variables as they will be public
# 3. For Vercel deployment, set these in the Vercel dashboard
# 4. Use .env.local for local development (already in .gitignore)
#
